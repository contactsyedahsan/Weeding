Decorative 3D render PNGs (ring, perfume, wedding objects).

Drop a PNG here, then in js/config.js find or copy its entry in
WI.assets3d, set "src" to the filename and "enabled: true".

Transparent background PNGs work best. Keep each file under ~400 KB
so the invitation still opens fast over mobile data.
