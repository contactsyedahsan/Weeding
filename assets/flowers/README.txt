Floral PNG assets.

Same workflow as assets/3d/ - register the file in WI.assets3d in
js/config.js with layer set to the section you want it in.

The site already draws its own SVG botanicals, so anything here is
additive rather than required.
